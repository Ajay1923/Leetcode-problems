class Solution{    
public:
    string findDifferentBinaryString(vector<string>& nums) {
        string k;
        int o =nums.size();
        for(int i=0;i<o;i++){
            if(nums[i][i]=='0')
                k+='1';
            else
                k+='0';
        }
        return k;
    }
};
